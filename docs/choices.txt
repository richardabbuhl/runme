A file called choices.txt that containing pure ASCII (not a word processor format) text describing the significant design choices
you made. Detail the problems you perceived, the issues surrounding them, your value judgments, and the decisions that you made.
This document should also describe any uncertainties you had regarding the project, and the decisions you made when resolving them.

1.  Choose RandomAccessFile since seeks are necessary to read/write files.

2.  Choose to cache the schema.

3.  Choose to use a HashMap for holding the lock.

4.  For the GUI, a simple approach was chosen so that search, modify, and deletes were allowed.

5.  Change:  However, sockets require the client and server to engage in applications-level protocols to encode and decode messages for exchange, and the design of such protocols is cumbersome and can be error-prone.